const Upgrade = {
    "projects": {
        "instructions": "TEXT NOT NULL DEFAULT ''",
        "last_used": "INTEGER NOT NULL DEFAULT 0",
        "icon": "TEXT NOT NULL DEFAULT 'mdi-folder-outline'",
        "icon_color": "TEXT NOT NULL DEFAULT 'primary'",
    },
}

export default Upgrade;